# UI 仕様

## 1. ウィンドウ構成

```
┌─────────────────────────────────────────────────────────────────┐
│ mdpilot - <プレビューファイル名>                           [_][□][×] │
├──────────────────────────────────┬──────────────────────────────┤
│                                  │                              │
│                                  │                              │
│   左ペイン                       │   右ペイン                   │
│   Markdown プレビュー            ║   内蔵ターミナル              │
│                                  │   (claude を対話的に動かす)   │
│                                  │                              │
│                                  │                              │
│                                  │                              │
│                                  │                              │
└──────────────────────────────────┴──────────────────────────────┘
                                  ↑
                          リサイズ可能な境界
```

- 単一ウィンドウ・2 ペイン構成
- 左ペイン: Markdown プレビュー（詳細は `preview.md`）
- 右ペイン: 内蔵ターミナル（詳細は `terminal.md`）
- 中央の境界はマウスドラッグでリサイズ可能

## 2. ウィンドウの既定値

| 項目 | 値 |
|---|---|
| 初期幅 | 1400 px |
| 初期高さ | 900 px |
| 最小幅 | 800 px |
| 最小高さ | 500 px |
| ペイン初期比率 | プレビュー : ターミナル = 1 : 1 |
| 起動位置 | OS のデフォルト（前回位置の復元は MVP 後） |

数値は MVP の暫定値。設定で上書きできるかは設定ファイル仕様の確定後に決定。

## 3. ペイン操作

| 操作 | 動作 |
|---|---|
| 境界をマウスドラッグ | 左右ペイン幅を変更 |
| 境界をダブルクリック | ペイン比率を 1 : 1 にリセット |
| ペイン最小幅 | 各ペイン 240 px。これ以下に縮められない |

## 4. ウィンドウタイトル

書式: `mdpilot - <プレビュー中ファイルのファイル名>`

- プレビュー対象未指定時: `mdpilot`
- ファイルが未保存変更を持つ概念は存在しない（手動編集機能を持たないため `*` などの印は付けない）

## 5. メニュー

UI の表示言語は **日本語**。メニュー項目・ダイアログ・ステータス表示はすべて日本語で記述する。

### 5.1 macOS（メニューバー）

| メニュー | 項目 | アクション |
|---|---|---|
| mdpilot | mdpilot について | バージョン情報ダイアログ |
|  | 環境設定... | 設定ファイルを既定エディタで開く（MVP 後） |
|  | mdpilot を終了 (`Cmd+Q`) | 終了 |
| ファイル | 開く... (`Cmd+O`) | ファイル選択ダイアログ。選んだ .md をプレビュー対象に設定 |
| 表示 | プレビューを再読込 (`Cmd+R`) | プレビュー強制再読込 |
|  | 拡大 (`Cmd+=`) / 縮小 (`Cmd+-`) | 表示倍率変更（MVP 後） |
| ウインドウ | しまう (`Cmd+M`) | OS 標準 |
|  | 拡大/縮小 | OS 標準 |
| ヘルプ | mdpilot について / GitHub | リンク |

### 5.2 Windows

メニューバーは MVP では持たない（egui のフレーム内に最小限の操作を配置）。代替として：

- ウィンドウ上部に小さなツールバー（開く / 再読込 / 情報）を置く
- もしくは右クリックでコンテキストメニュー

詳細は MVP 実装着手時に決定。

## 6. キーバインド

### 6.1 フォーカスペインによる解釈の切替

ターミナルとプレビューでは同じキーが別の意味を持つため、**フォーカスのあるペインによってキー解釈を変える** ことを原則とする。

| キー | ターミナルフォーカス時 | プレビューフォーカス時 |
|---|---|---|
| `Cmd+C` (mac) / `Ctrl+Shift+C` (Win) | 選択範囲をコピー | 選択範囲をコピー |
| `Ctrl+C` (両 OS、ターミナル内) | シェル/claude に `SIGINT` 相当を送る | 該当なし |
| `Cmd+V` (mac) / `Ctrl+Shift+V` (Win) | クリップボードからペースト | 該当なし |
| `Cmd+F` (mac) / `Ctrl+F` (Win) | ターミナル内検索（MVP 後） | プレビュー内検索（MVP 後） |

### 6.2 アプリ全体のキーバインド

| キー | アクション |
|---|---|
| `Cmd+Q` (mac) / `Alt+F4` (Win) | アプリ終了 |
| `Cmd+O` (mac) / `Ctrl+O` (Win) | ファイル選択ダイアログを開く |
| `Cmd+R` (mac) / `Ctrl+R` (Win) | プレビュー強制再読込 |
| `Cmd+\` (mac) / `Ctrl+\` (Win) | ペイン境界を 1 : 1 にリセット |

カスタマイズは MVP 後。

## 7. フォーカスの初期状態

- 起動時は **右ペイン（ターミナル）にフォーカス**。すぐに `claude` を打ち始められる
- ペインのクリックでフォーカス移動
- `Tab` キーでフォーカス循環するかは MVP では実装しない（混乱を招くため）

## 8. テーマ

- 既定: **OS のダークモード設定に追従**（`eframe::App` の `dark_mode_or_system` 相当）
- 強制的にライト/ダークを選ぶオプションは MVP 後
- プレビュー側の Markdown スタイルもテーマに連動する（背景色・コードブロック背景色）

## 9. ステータス表示

ウィンドウ下端にステータスバーを置くかは未確定。最低限の以下情報は表示したい：

- プレビュー対象ファイルのパス（フル）
- ファイル監視の状態（監視中 / エラー）
- 直近のエラー（あれば）

MVP では「左ペイン上部の小さなパスバー」+「エラー時のみトースト」程度で始める。

## 10. 既知の課題・未確定事項

| 項目 | 状態 |
|---|---|
| ペイン分割を上下に切り替える機能 | 未実装 |
| 複数ターミナル・複数プレビュー（タブ） | MVP 後 |
| 設定ダイアログ（GUI） | MVP 後 |
| 英語 UI 化 | 未対応（日本語のみ）。MVP 後の検討 |
| Windows のメニューバー or ツールバー | 実装着手時に決定 |
| ペイン比率の永続化 | MVP 後 |
